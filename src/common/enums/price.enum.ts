export enum PriceType {
    one_time = 'one_time',
    recurring = 'recurring',
  }

export enum RecurringInterval {
    day = 'day',
    week = 'week',
    month = 'month',
    year = 'year',
  }
  
